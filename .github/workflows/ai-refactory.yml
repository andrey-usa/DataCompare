name: AI Refactor (retired)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow retired
        run: echo "The automated AI refactor workflow has been retired."
